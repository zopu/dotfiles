# Commit changes and file a PR

## Ensure any relevant checks before commit are done.

Follow any instructions in CLAUDE.md for:
- Running all tests
- Formatting code
- Linting

## Commit and file a PR
- ensure all appropriate files are git staged 
- git commit all changes
- Use the github CLI to file a pr against main/master (whatever the default branch is)
- open the pr in my web browser

Any specific requests: $ARGUMENTS
